# Configuration de production - Seller Gest

# Port du serveur (défaut: 4000)
PORT=4000

# Secret pour les tokens JWT (IMPORTANT: utilisez une clé complexe en production!)
JWT_SECRET=votre_secret_jwt_tres_complexe_ici

# Base de données PostgreSQL (optionnel - utilise SQLite par défaut)
# DATABASE_URL=postgres://user:password@host:port/database

# Compte Super Administrateur (créé au démarrage s'il n'existe pas)
ADMIN_EMAIL=admin@votredomaine.com
ADMIN_PASSWORD=votre_mot_de_passe_admin
